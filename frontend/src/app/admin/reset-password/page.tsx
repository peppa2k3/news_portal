"use client";
import { useSearchParams } from "next/navigation"; import { useState } from "react"; import { adminApi } from "@/services/admin-api";
export default function Page() { const token = useSearchParams().get("token") || ""; const [message, setMessage] = useState(""); async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); const password = new FormData(e.currentTarget).get("password"); try { await adminApi("/auth/reset-password", { method: "POST", body: JSON.stringify({ token, password }) }); setMessage("Đổi mật khẩu thành công. Bạn có thể đăng nhập."); } catch (error) { setMessage(error instanceof Error ? error.message : "Không thể đổi mật khẩu"); } } return <main className="admin-root flex min-h-screen items-center justify-center bg-slate-950 px-4"><form onSubmit={submit} className="w-full max-w-md rounded-2xl bg-white p-7"><h1 className="text-3xl font-black">Đặt lại mật khẩu</h1><label className="field-label mt-6">Mật khẩu mới<input className="field-input" type="password" name="password" minLength={8} required /></label><button disabled={!token} className="btn-primary mt-5 w-full">Đổi mật khẩu</button>{message && <p className="mt-4 text-sm" role="status">{message}</p>}</form></main>; }
