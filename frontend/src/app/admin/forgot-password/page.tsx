"use client";
import { useState } from "react"; import { adminApi } from "@/services/admin-api";
export default function Page() { const [message, setMessage] = useState(""); async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); const email = new FormData(e.currentTarget).get("email"); try { await adminApi("/auth/forgot-password", { method: "POST", body: JSON.stringify({ email }) }); } finally { setMessage("Nếu email tồn tại, hướng dẫn đặt lại mật khẩu sẽ được gửi."); } } return <main className="admin-root flex min-h-screen items-center justify-center bg-slate-950 px-4"><form onSubmit={submit} className="w-full max-w-md rounded-2xl bg-white p-7"><h1 className="text-3xl font-black">Quên mật khẩu</h1><label className="field-label mt-6">Email<input className="field-input" type="email" name="email" required /></label><button className="btn-primary mt-5 w-full">Gửi hướng dẫn</button>{message && <p className="mt-4 text-sm" role="status">{message}</p>}</form></main>; }
